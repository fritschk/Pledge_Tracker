<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
		</ul>
		</div>
  <% end %>
  
  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :ecard, :value => ' ' %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :ecard, :value => ' '%>
  </div>
 <% end %>
	
<%= link_to "Login", new_post_path %>
</br>
</br>
<%= link_to "I'm New!", new_post_path %>